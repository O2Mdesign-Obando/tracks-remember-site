---
interface Props {
  title?: string;
  className?: string;
}

const { title, className = '' } = Astro.props;
---

<section class={`section ${className}`}>
  <div class="container">
    {title && <h2>{title}</h2>}
    <slot />
  </div>
</section>